**AI Disclaimer**: all of the C code is written by me, but AI helped a lot. This project was intended to help me learn C as well as how HTTP servers work. I progressed mainly by prompted an AI, "Show me the minimal toy example that does X in C", studying what it said, taking notes, and transcribing the code it provided. This was how I got to a synchronous HTTP loop, and then an asynchronous one, how I learned about sockets, epoll, eventfd, multithreaded execution, and the callback/handler pattern for processing requests and sending out responses. Beyond that, given my experience working with FastAPI, I was familiar with what needed to happen within the callback/handler pattern, so all of that code was me.
